import React, { FC } from 'react';
import { BrowserRouter as Router, Switch } from 'react-router-dom';

export const App: FC = () => {
    return (
        <Router>
            <Route exact
        </Router>
    )
}